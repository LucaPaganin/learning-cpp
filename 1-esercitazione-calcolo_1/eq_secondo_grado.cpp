#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b, c;

    cout << "Inserisci i coefficienti a, b, c: " << endl;
    cin >> a >> b >> c;

    if (a == 0) {
        // Equazione di primo grado: bx + c = 0
        // Scrivi tu qui - attenzione: anche b potrebbe essere 0!

    } else {
        // Equazione di secondo grado
        double delta = b*b - 4*a*c;

        // Scrivi tu qui i tre sotto-casi su delta

    }

    return 0;
}